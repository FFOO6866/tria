# Docker Environment Configuration Template
# ============================================================================
# SECURITY: Copy to .env.docker and fill in actual values
# This file is a template - .env.docker should NOT be committed to git
# ============================================================================

# ============================================================================
# DATABASE CREDENTIALS (PRODUCTION)
# ============================================================================
# SECURITY: Generate strong, unique passwords for each environment
# Use: openssl rand -base64 32
POSTGRES_USER=tria_admin
POSTGRES_PASSWORD=
POSTGRES_DB=tria_aibpo
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Full connection string
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# ============================================================================
# API KEYS (PRODUCTION)
# ============================================================================
# SECURITY: Never use demo/test keys in production
OPENAI_API_KEY=
XERO_CLIENT_ID=
XERO_CLIENT_SECRET=
XERO_REFRESH_TOKEN=
XERO_TENANT_ID=

# ============================================================================
# SECURITY KEYS
# ============================================================================
# Generate with: openssl rand -hex 32
SECRET_KEY=

# ============================================================================
# INSTRUCTIONS
# ============================================================================
# 1. Copy: cp .env.docker.example .env.docker
# 2. Fill all empty values with production credentials
# 3. NEVER commit .env.docker to git
# 4. Start with: docker-compose --env-file .env.docker up
